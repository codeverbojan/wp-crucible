name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  test:
    name: Test (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        php: ["8.1", "8.2", "8.4"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          tools: composer
          coverage: none

      - name: Get Composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> "$GITHUB_OUTPUT"

      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ matrix.php }}-${{ hashFiles('composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-${{ matrix.php }}-

      - name: Install Composer dependencies
        run: composer install --no-interaction --prefer-dist

      - name: PHPCS
        run: composer lint

      - name: PHPStan
        run: composer analyze

      - name: PHPUnit
        run: composer test

  plugin-check:
    name: WordPress Plugin Check
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Build assets
        run: npm run build

      - name: Plugin Check
        uses: wordpress/plugin-check-action@v1
        with:
          build-dir: "."

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [test, plugin-check]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Build distribution zip
        run: bash bin/build-zip.sh

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: starter-plugin-${{ steps.version.outputs.version }}.zip
          generate_release_notes: true

  # Uncomment to enable automatic deployment to WordPress.org SVN.
  # Requires WORDPRESS_ORG_SVN_USERNAME and WORDPRESS_ORG_SVN_PASSWORD
  # secrets to be configured in the repository settings.
  #
  # deploy:
  #   name: Deploy to WordPress.org
  #   runs-on: ubuntu-latest
  #   needs: release
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: "20"
  #         cache: npm
  #
  #     - name: Install npm dependencies
  #       run: npm ci
  #
  #     - name: Build assets
  #       run: npm run build
  #
  #     - name: Deploy to WordPress.org
  #       uses: 10up/action-wordpress-plugin-deploy@v2
  #       env:
  #         SVN_USERNAME: ${{ secrets.WORDPRESS_ORG_SVN_USERNAME }}
  #         SVN_PASSWORD: ${{ secrets.WORDPRESS_ORG_SVN_PASSWORD }}
  #       with:
  #         generate-zip: false
